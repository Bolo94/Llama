@using Microsoft.AspNetCore.Identity;
@using Microsoft.AspNetCore.Identity.EntityFrameworkCore;

@ViewBag.userSvg
@inject UserManager<ApplicationUser> UserManager;


@{

    var webRoot = @ViewBag.FilePath;

    var userSvgHtml = @ViewBag.userSvg;

    var userIdStr = @ViewBag.userIdStr;

   // var realUserId = UserManager.FindById()


    var filePath = System.IO.Path.Combine(webRoot, @userIdStr); 

            
   
   System.IO.File.WriteAllText(filePath, userSvgHtml);



}